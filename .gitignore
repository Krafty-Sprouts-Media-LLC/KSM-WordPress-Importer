# Package Managers
/vendor
/node_modules

# Fresh copy reference — local only, not part of the plugin
.freshcopy/

# Duplicate reference trees — canonical copy is under .legacy/reference/
legacy/
WordPress-Importer-master/

# Test XML files outside fixtures — not part of the plugin
*.xml
!tests/fixtures/*.xml

# Local development
.DS_Store
Thumbs.db
test-results/
wxr-upload-debug.log
healthtian.WordPress.*.xml
